import { DoublyList, DoublyNode } from '@/day07-linked-list/DoublyLinkedList';

const useByInsert = Symbol('useByInsert');

export class SortedList extends DoublyList {
  find(value, second) {
    // your code here
  }

  insert(value) {
    // your code here
  }

  remove(value) {
    // your code here
  }
}
